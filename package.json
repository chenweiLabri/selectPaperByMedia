{
  "name": "selectpaper",
  "version": "1.0.0",
  "description": "compare the titles of the references with the original papers to know which papers are not be read and select the papers by media",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "wei chen",
  "license": "ISC",
  "dependencies": {
    "does-include": "^1.0.0",
    "fs": "0.0.1-security",
    "request": "^2.81.0"
  }
}
